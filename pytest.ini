[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
asyncio_mode = auto

addopts =
    -v
    --tb=short
    --cov=app
    --cov=pipeline
    --cov-report=term-missing
    --cov-fail-under=70