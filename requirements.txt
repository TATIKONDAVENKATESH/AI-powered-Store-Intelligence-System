# ── Web API ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.9

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0

# ── Computer Vision ────────────────────────────────────────────────────────
# NOTE: torch (CPU) is installed BEFORE this file in Dockerfile to avoid
# ultralytics pulling the 2GB CUDA wheel automatically.
# Local install (no Docker): run this first:
#   pip install torch==2.3.0+cpu torchvision==0.18.0+cpu --index-url https://download.pytorch.org/whl/cpu
ultralytics==8.2.18
supervision==0.20.0
opencv-python-headless==4.9.0.80
numpy==1.26.4

# ── Data / Dashboard ───────────────────────────────────────────────────────
pandas==2.2.2
streamlit==1.35.0
requests==2.32.2
httpx==0.27.0

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0